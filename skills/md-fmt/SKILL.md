---
name: md-fmt
description: |
  对单个 Markdown 文件进行一站式标准化：先排版规范化，再网络图片本地化。当用户要求"规范化 markdown"、"标准化 markdown"、"离线化 markdown"、"本地化 markdown 图片并整理排版"时触发。只要用户提到对 markdown 做排版整理和图片下载的组合操作，就应该使用这个技能，即使用户没有明确说"标准化"。
  仅处理单个文件。如果用户提供了多个文件或通配符，应引导用户使用 `batch-md-fmt` 技能。
---

当此 skill 生效时，回答第一行固定写：Using skill: md-fmt

> **注意**：本技能仅处理单个文件。如果用户提供了多个文件路径或通配符（如 `*.md`、`**/*.md`），请告知用户并改为调用 `batch-md-fmt` 技能。

## 处理流程

对用户提供的单个 Markdown 文件，严格按以下顺序执行两个阶段。

### 计时

在每个阶段**开始前**和**结束后**，各执行一次 `date +%s` 记录 Unix 时间戳，用于计算该阶段耗时（秒）。完成报告中需包含各阶段耗时。

### 阶段 1：排版规范化

使用 Skill 工具调用 `md-zh` 技能，对目标文件应用中文排版规范。具体做法：

1. 读取目标 Markdown 文件全文
2. 调用 `md-zh` 技能加载排版规则
3. 按照 md-zh 的规则逐项检查并修改文件内容，包括但不限于：
   - 中英文之间加空格
   - 中文与数字之间加空格
   - 使用全角中文标点
   - 专有名词大小写
   - 标点符号规范
4. 将修改后的内容写回原文件
5. 告知用户排版规范化已完成

### 阶段 2：图片本地化

使用 Skill 工具调用 `md-img-local` 技能，将文件中的网络图片下载到本地。具体做法：

1. 调用 `md-img-local` 技能
2. 按照该技能的流程，扫描文件中的网络图片链接，下载到 assets 目录，替换为本地相对路径
3. 告知用户图片本地化已完成，报告下载结果

### 完成报告

两个阶段都完成后，向用户汇总：
- 排版修改了哪些方面
- 下载了多少张图片、保存位置
- 如有图片下载失败，列出失败的链接
- 排版耗时：X 秒
- 图片耗时：Y 秒

---

## 边界情况

- **文件中没有网络图片**：正常执行阶段 1，阶段 2 告知用户无需下载图片
- **文件排版已经规范**：阶段 1 告知用户无需修改，继续执行阶段 2
- **用户只想做其中一项**：如果用户只要排版不要图片（或反过来），引导用户直接使用对应的单独技能（`md-zh` 或 `md-img-local`）
- **用户提供了多个文件**：告知用户本技能仅处理单文件，引导使用 `batch-md-fmt` 技能
